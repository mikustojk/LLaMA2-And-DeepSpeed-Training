# 动手搭建 LLaMA2 项目方案

更新时间：2026-08-03

## 1. 项目目标

手动实现一个轻量级 LLaMA2 风格的 Decoder-only 语言模型，完成 Tokenizer、预训练、SFT 和评估流程，并系统比较 Plain PyTorch、DeepSpeed 和 Megatron 三种训练方案在速度、显存、吞吐和模型效果上的差异。

学习报告作为统一下游任务。输入由程序提供，包括总学习时长、有效学习时长和分心事件次数。程序先计算有效率、基础分、扣分、最终分和等级，再由模型生成 AI 分析和 AI 建议。嵌入式部署不再属于本项目的主要目标。

当前阶段优先完成 Plain PyTorch 与 DeepSpeed 的公平对比。Megatron 保留在总体目标中，但暂缓到前两种方案的基准和结果分析完成之后。

## 2. 总体流程

```text
训练或准备 Tokenizer
        ↓
处理预训练数据
        ↓
随机初始化等价 LLaMA2 配置
        ↓
Plain PyTorch / DeepSpeed / Megatron 预训练
        ↓
分别保存三种方案的 Base checkpoint
        ↓
使用同一份指令数据和 loss mask 进行三种方案的 SFT
        ↓
统一测试学习报告生成效果
        ↓
比较速度、显存、吞吐、loss 和报告质量
        ↓
可选：DPO 和推理优化
```

## 3. 已完成内容

### 3.1 模型结构

- [x] 实现 RMSNorm。
- [x] 实现 RoPE，包括频率预计算、广播形状调整和 Q/K 旋转。
- [x] 实现 GQA，包括 `n_kv_heads` 和 `repeat_kv`。
- [x] 实现 SwiGLU MLP，包括 `w1`、`w2`、`w3` 和 SiLU 激活。
- [x] 实现 Pre-Norm、残差连接、DecoderLayer 和完整 Transformer。
- [x] 使用因果注意力，支持 PyTorch scaled dot-product attention。
- [x] 实现逐 token loss，并支持 loss mask。

当前模型主要配置为：

```text
dim=1024
n_layers=18
n_heads=16
n_kv_heads=8
vocab_size=6144
max_seq_len=512
dropout=0
parameter_count=215127040（约 215.1M）
```

对应代码为 [LLaMA2.py](<D:\\bupt\\多模态大模型\\动手搭建LLaMA2\\LLaMA2.py>)。

### 3.2 Tokenizer 和数据集

- [x] 完成 Byte-Level BPE Tokenizer 训练代码。
- [x] 准备 `tokenizer_k`，词表大小为 6144。
- [x] 实现 `PretrainDataset`，完成 JSONL 按行定位、Token ID 转换、X/Y 位移和 padding mask。
- [x] 实现 `SFTDataset`，使用对话模板，并通过 assistant 区间生成 loss mask。
- [x] 生成学习报告 SFT 数据，当前最终数据集为 `data/sft/learning_report_sft_v5_10000.jsonl`。
- [x] v5 数据包含 `reading` 和 `courseware` 两种学习模式，分析和建议文本进行了多样化处理。

### 3.3 训练

- [x] 完成 DeepSpeed 配置和训练入口。
- [x] `pretrain_ds.py` 已通过单步 smoke test，checkpoint 保存、恢复和旧 checkpoint 保留逻辑已验证。
- [x] 预训练曾进行长时间运行并生成多个 checkpoint，但完整大语料预训练尚未作为最终成果确认。
- [x] SFT 已完成 2500 steps，输出目录为 `checkpoints/sft_report_v5`。
- [x] 当前 SFT 脚本 [sft_ds.py](<D:\\bupt\\多模态大模型\\动手搭建LLaMA2\\sft_ds.py>) 使用：
  - 基础 checkpoint：`checkpoints/sft_report_v2/model.pth`
  - 数据集：`data/sft/learning_report_sft_v5_10000.jsonl`
  - 输出 checkpoint：`checkpoints/sft_report_v5/model.pth`

### 3.4 推理和测试

- [x] `test_sft.py` 已切换到 `checkpoints/sft_report_v5/model.pth`。
- [x] 测试输入先由程序计算分数，再将原始数据和计算结果一同输入模型。
- [x] 测试脚本当前只使用 `reading` 和 `courseware` 两种 mode。
- [x] 十条测试数据的数值字段均能由程序正确拼接，模型负责生成 AI 分析和 AI 建议。
- [ ] 当前 `generate` 没有实现 KV Cache，推理加速仍属于基础优化阶段。
- [ ] 尚未完成 DPO 训练和系统化自动评估。

## 4. 当前已知问题

1. 测试数据中的 mode 必须与 SFT 数据的分布一致。v5 只训练了 `reading` 和 `courseware`，不能用未训练的 `programming`、`vocabulary` 等 mode 来判断模型的模式理解能力。
2. 程序拼接的总时长、有效率、扣分和最终分应作为可信数值来源，模型只生成分析和建议，避免模型重新计算造成数值错误。
3. `LLaMA2.py` 中的 `flash_attention` 配置项已经接入 `self.flash` 判断；当前 benchmark 默认使用同一种 SDPA 实现，避免 Attention 实现差异干扰训练框架对比。
4. 当前 DeepSpeed 配置的全局 batch size 为 8，适用于单卡的 `micro_batch=1、gradient_accumulation=8`。切换到双卡时需要重新设置累积步数，不能直接沿用而改变有效 batch size。
5. 当前 `PretrainDataset` 每次取样都会读取 JSONL 并调用 tokenizer。进行 Plain PyTorch、DeepSpeed 和 Megatron 吞吐对比时，最好先把 token ID 预处理好，避免数据管道差异影响结果。
6. Megatron-LM 不能直接包裹当前 `Transformer`。需要使用 Megatron 的 GPTModel，或将线性层、Embedding、Attention 和 MLP 改造成张量并行版本。Megatron 实验的模型结构必须记录与当前模型的差异。
7. 历史 checkpoint 可能使用 `768/12`，而当前项目目标配置是 `1024/18`。`pretrain_ds.py` 现在默认显式使用 `1024/18/16/8`；恢复旧 checkpoint 时必须显式传入对应的旧模型参数，不能混用不同结构的 checkpoint。

## 5. 下一阶段计划

### P0：完成当前 SFT 结果评估

- [ ] 在服务器上运行 `test_sft.py` 的十条测试。
- [ ] 检查数值一致性：总时长、有效学习、有效率、基础分、扣分、最终分和等级。
- [ ] 检查文本质量：是否先分析有效率，再分析分心次数；建议是否保持具体、鼓励且不泄露无关数值。
- [ ] 增加自动检查脚本，验证模型生成结果中关键数值没有覆盖程序计算结果。

### P1：阶段一，Plain PyTorch 与 DeepSpeed 对比

先只比较预训练和 SFT 中的两种方案：

1. Plain PyTorch：使用当前 `Transformer` 和原生训练循环。
2. DeepSpeed：使用当前 `Transformer`，通过 `deepspeed.initialize` 接入 ZeRO-2。

当前已准备单卡预训练 benchmark：

- `benchmark/pretrain_plain_bench.py`
- `benchmark/pretrain_ds_bench.py`
- `benchmark/benchmark_utils.py`
- `benchmark/ds_config_bench.json`

benchmark 默认使用 `dim=1024、n_layers=18、n_heads=16、n_kv_heads=8`，预热 20 个 optimizer steps，正式测量 1000 个 optimizer steps；可以通过 `--measure_steps 10000` 进行长期稳定性测试。快速 smoke test 使用 `test_1000.jsonl`，正式比较必须通过 `--data_path` 指向完整 JSONL。两套 smoke test 已在服务器单卡完成，正式 1000/10000 steps 结果尚未完成。

固定模型、数据、精度、优化器、全局 batch size、序列长度和测量步数，记录：

- 平均每个 optimizer step 的时间。
- samples/s。
- tokens/s 和有效 tokens/s。
- 每张卡峰值 allocated/reserved 显存。
- loss 和显存随 step 的变化。

每个实验先预热 20 steps，再测量 1000 steps；正式结果至少重复 3 次并取中位数。需要长期稳定性时再测量 10000 steps。不要在测量区间保存 checkpoint、进行验证或大量打印。

### P1 smoke test 记录

2026-07-31 在同一项目配置下完成单卡、1 个预热 step 加 1 个测量 step 的 smoke test。原始结果保存在：

- `benchmark/results/plain_smoke_v2.json`
- `benchmark/results/deepspeed_smoke_v2.json`

两种方案的模型、Tokenizer、数据路径、BF16、AdamW、学习率、梯度裁剪、global batch、序列长度、随机种子和有效 token 数一致。结果如下：

| 指标 | Plain PyTorch | DeepSpeed ZeRO-2 | 差异 |
| --- | ---: | ---: | ---: |
| 每个 optimizer step | 0.5348 s | 0.5986 s | DeepSpeed 慢约 11.9% |
| nominal tokens/s | 7644 | 6830 | DeepSpeed 低约 10.7% |
| valid tokens/s | 6517 | 5822 | DeepSpeed 低约 10.7% |
| allocated 显存 | 4.165 GiB | 5.519 GiB | DeepSpeed 多约 1.35 GiB |
| reserved 显存 | 4.771 GiB | 8.293 GiB | DeepSpeed 多约 3.52 GiB |
| valid tokens | 3485 | 3485 | 一致 |
| 最后 loss | 8.2961 | 8.3125 | 接近 |

由于正式测量只有 1 step，这组数据只用于验证训练流程、参数对齐和性能方向，不能替代 1000 或 10000 steps 的正式结果。单卡下 ZeRO-2 没有多卡分摊收益，额外的分区管理和通信缓冲可能造成速度下降和显存增加。

### P1 Plain PyTorch 1000-step 基线

结果文件为 `benchmark/results/plain_full_1000.json`。该实验使用完整预训练 JSONL 作为数据源，预热 20 steps，正式测量 1000 steps；global batch size 为 8，因此正式区间处理 8000 条样本，并不等于完整语料跑完一轮。

| 指标 | Plain PyTorch 基线 |
| --- | ---: |
| 总测量时间 | 544.2035 s |
| 每个 optimizer step | 0.5442 s |
| nominal tokens/s | 7511.90 |
| valid tokens/s | 6070.54 |
| samples/s | 14.7004 |
| allocated 显存峰值 | 4.1638 GiB |
| reserved 显存峰值 | 4.8066 GiB |
| 最后 loss | 5.0586 |

该结果的配置字段完整，nominal token 数等于 `1000 × 8 × 511 = 4,088,000`，有效 token 数为 3,303,609。与 1-step smoke 相比，step 时间和显存基本稳定，可作为 DeepSpeed 1000-step 结果的对照基线；最终加速比仍待 DeepSpeed 使用相同命令参数完成后计算。

### P1 DeepSpeed 1000-step 结果与单卡结论

结果文件为 `benchmark/results/deepspeed_full_1000.json`。它与 Plain 基线使用相同的数据路径、Tokenizer、模型结构、BF16、AdamW、学习率、梯度裁剪、global batch、序列长度、随机种子和测量步数。

| 指标 | Plain PyTorch | DeepSpeed ZeRO-2 | DeepSpeed 相对 Plain |
| --- | ---: | ---: | ---: |
| 每个 optimizer step | 0.5442 s | 0.6950 s | 慢 27.7% |
| nominal tokens/s | 7511.90 | 5882.43 | 低 21.7% |
| valid tokens/s | 6070.54 | 4753.73 | 低 21.7% |
| samples/s | 14.7004 | 11.5116 | 低 21.7% |
| allocated 显存峰值 | 4.1638 GiB | 5.5187 GiB | 多 32.5% |
| reserved 显存峰值 | 4.8066 GiB | 8.2930 GiB | 多 72.5% |
| 最后 loss | 5.0586 | 5.1250 | 接近 |

当前实验是单 GPU、ZeRO-2、约 215M 参数规模，模型可以直接放入单卡显存，因此 DeepSpeed 没有获得多卡分片或显存压力缓解的收益，额外的 engine、梯度分区管理和通信缓冲反而带来开销。这个结果说明 DeepSpeed 在当前单卡条件下不占优，并不代表它在多 GPU 或更大模型上的表现；多 GPU 对比和显存受限场景尚未验证。

### P1.2 DeepSpeed 优势验证方案

第一步已经完成脚本配置、smoke test 和三组有效的首轮 1000-step 测试；重复实验待完成：

- 在单卡固定 global batch size 为 8，成对测试 Plain PyTorch 和 DeepSpeed 的 `micro/accumulation=1/8、2/4、4/2`。
- `1/8、2/4、4/2` 的首轮 1000-step 有效结果均已生成；`4/2` 已在同一张空闲 GPU 上顺序重跑，配置、计算和运行时间均未发现异常。
- 两份脚本会拒绝 micro batch 与累积步数乘积不等于 8 的配置。DeepSpeed 的命令行参数只覆盖当前进程内的配置字典，不修改 `ds_config_bench.json`。
- 本阶段用于判断更大的 micro batch 能否提高 GPU 利用率并减少梯度累积循环带来的开销。两种框架必须使用相同的 micro batch、global batch、数据、模型和测量步数。

第二步为双卡实验，脚本和首轮 1000-step 正式测试已经完成：

- 已新增 `benchmark/pretrain_plain_ddp_bench.py` 和 `benchmark/pretrain_ds_2gpu_bench.py`。
- Plain DDP 和 DeepSpeed ZeRO-2 必须同时使用两张 GPU，不能把双卡 DeepSpeed 与单卡 Plain PyTorch 直接比较。
- 两种方案均使用每卡 `micro_batch=4`、`gradient_accumulation_steps=1` 和 `world_size=2`，因此 global batch size 均为 8。
- Plain 脚本使用 DDP 和 `DistributedSampler`；DeepSpeed 脚本使用 ZeRO-2 和相同的 `DistributedSampler` 数据切分参数。
- 双卡吞吐使用最慢 rank 的耗时和两个 rank 的总 token 数计算。结果同时记录每个 rank 的峰值 allocated/reserved 显存。
- 双卡实验单独报告总吞吐、每卡峰值显存，以及相对单卡 `micro=4、accumulation=2` 的扩展倍数和扩展效率。

增加测量 steps 只会提高时间统计的稳定性，不会消除单卡 ZeRO-2 的额外开销；限制可用显存也不会让 DeepSpeed 自动变快。正式结果应重复多次并取中位数。

#### P1.3 双卡首轮 1000-step 结果

结果文件为 `benchmark/results/plain_ddp_2gpu_micro4_accum1_1000.json` 和 `benchmark/results/deepspeed_2gpu_micro4_accum1_1000.json`。两者均使用两张 GPU、每卡 `micro_batch=4`、`gradient_accumulation_steps=1` 和 global batch size 8，处理相同的 3,294,222 个有效 token。模型、数据、Tokenizer、BF16、AdamW、学习率、梯度裁剪、序列长度、随机种子和测量步数一致，派生指标均已复算通过。

| 指标 | Plain PyTorch DDP | DeepSpeed ZeRO-2 | DeepSpeed 相对 Plain |
| --- | ---: | ---: | ---: |
| 每个 optimizer step | 0.1335 s | 0.1460 s | 慢 9.4% |
| nominal tokens/s | 30,621.59 | 27,997.01 | 低 8.6% |
| valid tokens/s | 24,675.71 | 22,560.76 | 低 8.6% |
| 每卡 allocated 峰值 | 5.7402 GiB | 5.1641 GiB | 少 0.5761 GiB，约 10.0% |
| 每卡 reserved 峰值 | 6.1016 GiB | 5.8574 GiB | 少 0.2441 GiB，约 4.0% |
| 最后 loss | 4.7475 | 4.7656 | 相差 0.0182 |

相对旧单卡 `micro=4、accumulation=2` 结果，Plain DDP 的 nominal 吞吐扩展倍数为 1.631，扩展效率为 81.6%；DeepSpeed 为 1.815 和 90.8%。由于旧单卡结果生成于数据偏移修复之前，这两组扩展效率暂作为参考。双卡框架之间的直接比较不受该问题影响。首轮结果显示 DeepSpeed 的绝对吞吐仍低于 DDP，但每卡显存更少，且相对单卡的扩展效率更高。最终结论需要再重复两轮并取中位数。

“DeepSpeed 每卡显存更少但速度更慢”符合当前配置的运行机制。ZeRO-2 将优化器状态和梯度分片，因此双卡每个 rank 不再保存完整的这两部分状态；模型参数和激活仍保留在每张卡上，所以当前 215M 模型只节省约 10% allocated 显存。分片同时引入 reduce-scatter、参数同步和 DeepSpeed engine 管理开销。当前 global batch 和每卡 micro batch 已固定，节省出的显存没有用于增大模型、序列长度或 micro batch，因而不能转化为更多计算吞吐，最终表现为显存下降、速度略慢。该结果应表达为 ZeRO-2 的容量优势，而不是加速优势。

#### P1.4 下一轮容量与吞吐实验

下一轮实验分开验证稳定吞吐和容量优势，避免把不同目标混成一个结论：

1. 当前配置再重复两轮，取三次 1000-step 结果的中位数。单纯增加到 10000 steps 只用于检查长期稳定性，不作为让 DeepSpeed 变快的手段。
2. 在两种框架使用相同配置的前提下，将每卡 micro batch 从 4 依次提高到 8、16，梯度累积保持 1，对应 global batch size 为 16、32。该实验用于判断更大的单次计算量能否提高 GPU 利用率并摊薄通信开销。
3. 单独进行最大容量实验，逐步增加模型维度或层数，记录 Plain DDP 和 DeepSpeed 分别在哪个配置发生 OOM，以及各自最大可运行配置的吞吐。该实验用于体现 ZeRO-2 的显存容量优势，不能与固定配置速度对比混为一项。
4. 梯度累积只在单卡 micro batch 已受显存限制时使用。在固定 global batch 下，提高累积次数意味着降低 micro batch，通常不利于 GPU 利用率；在 micro batch 不变时提高累积次数又会改变 global batch 和优化过程，因此不作为下一轮首选变量。

当前双卡脚本固定 global batch size 为 8。执行 micro batch 扫描前，需要将目标 global batch 改为命令行参数，并继续强制校验 `micro_batch × accumulation × GPU 数量 = global batch`。该代码调整尚未执行。

模型规模实验首先使用 `dim=1536、n_layers=24、n_heads=24、n_kv_heads=8`，预计参数量为 613,492,224，约为当前 215.1M 基线的 2.85 倍。该配置保持 head dimension 为 64，并满足 `n_heads % n_kv_heads == 0`。两个双卡脚本已经增加运行时参数量统计，结果 JSON 将记录总参数量、可训练参数量和实际 MLP 隐藏维度。服务器 smoke test 尚未执行。

#### P1.2 `micro=4、accumulation=2` smoke test 记录

结果文件为 `benchmark/results/plain_micro4_smoke.json` 和 `benchmark/results/deepspeed_micro4_smoke.json`。两者均为单卡、预热 1 step、测量 1 step，并处理相同的 3485 个有效 token。模型、数据、精度、优化器、学习率、global batch、序列长度和随机种子一致。

| 指标 | Plain PyTorch | DeepSpeed ZeRO-2 | DeepSpeed 相对 Plain |
| --- | ---: | ---: | ---: |
| 每个 optimizer step | 0.2670 s | 0.2196 s | 少 17.8% |
| nominal tokens/s | 15308.27 | 18615.31 | 高 21.6% |
| valid tokens/s | 13050.22 | 15869.46 | 高 21.6% |
| allocated 显存峰值 | 5.7559 GiB | 6.7675 GiB | 多 1.0116 GiB |
| reserved 显存峰值 | 6.2012 GiB | 8.2305 GiB | 多 2.0293 GiB |
| 最后 loss | 8.3914 | 8.4375 | 相差 0.0461 |

这组结果证明 `micro=4` 可以在两种方案中正常运行，并显示 DeepSpeed 在该单步测量中吞吐更高。由于正式测量只有 1 step，速度差异只能作为方向性信号，不能替代 1000-step 结果。

#### P1.2 单卡 micro batch 首轮 1000-step 结果

六份结果的模型、数据、Tokenizer、BF16、AdamW、学习率、梯度裁剪、global batch size、序列长度、随机种子、测量步数和有效 token 数一致。所有吞吐指标均可以由 token 数、样本数和 elapsed time 复算，未发现 JSON 字段或计算错误。

| micro / accumulation | Plain nominal tokens/s | DeepSpeed nominal tokens/s | DeepSpeed 相对 Plain | Plain allocated | DeepSpeed allocated |
| --- | ---: | ---: | ---: | ---: | ---: |
| `1 / 8` | 7511.90 | 5882.43 | 低 21.7% | 4.1638 GiB | 5.5187 GiB |
| `2 / 4` | 15972.65 | 11126.71 | 低 30.3% | 4.6948 GiB | 5.9359 GiB |
| `4 / 2` | 18773.98 | 15424.93 | 低 17.8% | 5.7536 GiB | 6.7675 GiB |

首轮有效结果中，两种框架的最高吞吐都出现在 `micro=4、accumulation=2`。相对 `1/8`，Plain 吞吐提高 149.9%，DeepSpeed 提高 162.2%；相对 `2/4`，Plain 提高 17.5%，DeepSpeed 提高 38.6%。显存随 micro batch 增大而上升。`4/2` 下 DeepSpeed 比 Plain 的 step time 多 21.7%、吞吐低 17.8%，并多使用 1.0139 GiB allocated 和 2.0293 GiB reserved 显存。

有效的 `4/2` 结果来自 `plain_micro4_accum2_1000_rerun1.json` 和 `deepspeed_micro4_accum2_1000_rerun1.json`。两份文件生成时间与顺序执行一致，共同配置、token 数和指标公式均正确。此前并发运行得到的两份无后缀文件继续保留作为无效实验记录，不参与统计。当前每组有效配置仍只有一次正式运行；若要形成最终性能结论，至少应将选中的 `4/2` 配置再重复两次并取中位数。

上述单卡结果生成时，`PretrainDataset` 的偏移数组会重复读取第一条样本。Plain 和 DeepSpeed 使用了相同的数据读取逻辑，因此同一组单卡框架对比仍可用于吞吐判断；若要精确计算双卡相对单卡的扩展效率，应在修复后的数据集代码上补跑一组单卡 `micro=4、accumulation=2` 基线。

本阶段不启动 Megatron。完成 Plain PyTorch 与 DeepSpeed 后，再单独设计 Megatron 的等价配置或移植方案。

### P1.1：Megatron 后续阶段

- [ ] 使用 Megatron 的等价 GPT 配置，或将当前模型移植到 Megatron Core。
- [ ] 确认模型结构、Tokenizer、数据格式和并行方式的差异。
- [ ] 在预训练和 SFT 阶段加入 Megatron 对比。

### P2：DPO 数据和训练

- [ ] 为同一条学习数据构造 chosen/rejected 建议对。
- [ ] chosen 应该数值正确、分析顺序合理、建议具体且鼓励。
- [ ] rejected 应包含数值错误、忽略分心事件、模式不匹配或建议过度负面的样本。
- [ ] 在 SFT checkpoint 上进行 DPO，并单独比较 SFT 与 DPO 的十条固定测试。

### P3：可选训练扩展

- [ ] 在三种 SFT 模型上构造并运行 DPO。
- [ ] 实现 KV Cache，比较生成速度和显存。
- [ ] 测试 BF16、FP16、INT8 或更小模型配置。

## 6. 常用命令

在服务器项目目录 `/mnt/0608/guohyx/LLaMA2` 下执行。

### 预训练 smoke test

```bash
CUDA_VISIBLE_DEVICES=0 uv run deepspeed pretrain_ds.py \
  --data_path data/seq_monkey/test_1000.jsonl \
  --max_steps 1 \
  --save_every 0 \
  --save_dir checkpoints/pretrain_smoke
```

### SFT

```bash
CUDA_VISIBLE_DEVICES=0 uv run deepspeed sft_ds.py
```

### SFT 测试

```bash
CUDA_VISIBLE_DEVICES=0 uv run python test_sft.py
```

只测试第一个案例：

```bash
CUDA_VISIBLE_DEVICES=0 uv run python test_sft.py --case 1
```

## 7. 项目成果表达

简历可以写：

> 熟悉 LLaMA2 解码器架构，掌握 RoPE、SwiGLU、GQA 等核心组件原理；具备基于 DeepSpeed 完成预训练与 SFT 的全流程训练经验，完成学习报告数据构建、Loss Masking、checkpoint 管理和模型评估。

面试重点准备：

1. LLaMA2 的数据流和 DecoderLayer 结构。
2. GQA 如何通过减少 KV head 降低 KV Cache 和注意力计算开销。
3. SFT loss mask 为什么只训练 assistant 回复。
4. DeepSpeed ZeRO-2 如何切分梯度和优化器状态。
5. 为什么程序计算数值、模型生成文本。
6. 训练速度、显存和吞吐如何公平测量。

## 8. 变更记录

- 2026-08-03：新增 Plain PyTorch DDP 和 DeepSpeed ZeRO-2 双卡预训练 benchmark，固定两边 global batch size 为 8，按 rank 切分数据，并汇总最慢 rank 耗时、总有效 token 和每卡峰值显存；5 个相关 Python 文件已通过 AST 检查，DeepSpeed JSON 和 README 命令也已通过静态检查，脚本等待服务器双卡 smoke test。
- 2026-08-03：复查双卡 benchmark，补充 `--local-rank` 与 `--local_rank` 启动参数兼容；修正 `PretrainDataset` 重复保存首行偏移导致第一条样本重复、数据集长度多 1 的错误。修复后的 Python 语法、JSON、偏移读取、全局 batch、分布式采样、梯度同步和指标聚合检查均已通过，等待服务器运行验证。
- 2026-08-03：完成首轮双卡 1000-step 正式测试。Plain DDP 为 30,621.59 nominal tokens/s、5.7402 GiB 每卡 allocated；DeepSpeed ZeRO-2 为 27,997.01 tokens/s、5.1641 GiB。DeepSpeed 吞吐低 8.6%，每卡 allocated 显存少 10.0%，相对旧单卡结果的扩展效率更高；等待另外两轮重复实验。
- 2026-08-03：确认双卡结果中的“DeepSpeed 显存较少但吞吐较低”来自 ZeRO-2 的状态分片与额外通信开销；当前节省的显存没有用于扩大模型或单卡工作量，因此记录为容量优势，不表述为绝对训练加速。
- 2026-08-04：再次确认双卡 215M 实验中 DeepSpeed ZeRO-2 每卡 allocated 显存下降约 10%，同时吞吐下降约 8.6%，属于当前模型规模下状态分片收益与通信、框架管理开销之间的正常权衡。
- 2026-08-04：简历数据暂按已验证的 2.15 亿参数双卡 benchmark 表述：DeepSpeed ZeRO-2 相比 Plain DDP 每卡 allocated 显存降低 10.0%，训练吞吐下降 8.6%；在更大模型结果完成前，不写成吞吐提升或完整预训练。
- 2026-08-04：双卡吞吐下降的量化原因记录：两组实验每步处理的 nominal token 相同，Plain DDP 为 0.1335 s/step，DeepSpeed ZeRO-2 为 0.1460 s/step；ZeRO-2 的梯度切分、聚合、参数同步和框架管理开销使单步耗时增加约 9.4%，按吞吐与单步耗时成反比计算，对应吞吐下降约 8.6%。215M 模型计算量较小，且节省的显存未用于增加 micro batch、序列长度或模型规模，因此这些开销尚未被额外计算量抵消。
- 2026-08-04：将简历中的“预训练与分布式训练”和“SFT 与对齐”合并为“预训练与 SFT”，分布式训练优化单独成点；合并项保留 Tokenizer、预训练到 SFT 的流程、对话数据、chat template、loss mask、checkpoint 和权重导出等核心内容。
- 2026-08-04：开始准备将项目发布到 GitHub；本地项目尚未初始化 Git，当前环境未安装 GitHub CLI `gh`。初步上传范围为源码、benchmark、配置、文档、最终 SFT 数据和少量结果 JSON，排除完整预训练语料、模型权重、checkpoint、虚拟环境及旧版中间数据，待完成 CLI 认证后推送。
- 2026-08-04：确认 GitHub 空仓库页面提供的 `git init`、`git remote add`、`git push` 是直接上传本地项目的标准流程；`gh` 只用于 GitHub 认证、仓库信息和发布流程管理，不替代这些 Git 命令。后续按已确认的文件范围执行上传。
- 2026-08-04：按官方 Git 流程完成首次上传，提交 `44b5e99` 已推送到 `mikustojk/LLaMA2-And-DeepSpeed-Training` 的 `main` 分支。仓库包含 46 个源码、文档、benchmark、配置、结果和最终 SFT 数据文件；旧版 `sft_ds（old）.py` 与空文件 `test_pretrain` 保留在本地但未上传。
- 2026-08-04：按用户要求将内部协作说明、旧版学习报告生成脚本和旧文档从 Git 跟踪中移除，保留本地副本供项目继续使用，并加入 `.gitignore` 防止再次上传。
- 2026-08-05：按用户要求删除 README 末尾“数据和模型权重”部分，仅保留项目内容、当前配置和性能对比说明。
- 2026-08-03：确定下一轮实验顺序：先重复当前双卡基线，再进行 `micro_batch=8、16` 的成对吞吐测试，最后增加模型规模测试最大可运行配置；更多 steps 和更高梯度累积不作为制造加速效果的主要手段。相关脚本参数化尚未执行。
- 2026-08-03：记录当前基线参数量为 215,127,040，并选择 613,492,224 参数的 `1536/24/24/8` 配置作为首个模型放大实验；双卡脚本已增加实际参数量和 MLP 隐藏维度记录，README 已加入 smoke 与 1000-step 命令，等待服务器验证。
- 2026-07-31：建立项目方案，记录模型、Tokenizer、预训练、SFT、评估和三框架性能对比计划。
- 2026-07-31：新增 `AGENTS.md`，规定后续协作、代码修改、训练实验和方案维护规则。
- 2026-07-31：项目范围调整为三种训练方案的预训练与 SFT 对比，移除嵌入式部署作为主要目标。
- 2026-07-31：确认 DeepSpeed 可在现有 `Transformer` 外层接入；Megatron 需要使用等价 GPTModel 或移植张量并行模型，不能只增加一个开关。
- 2026-07-31：确定当前优先完成 Plain PyTorch 与 DeepSpeed 的预训练、SFT 和性能对比，Megatron 暂缓。
- 2026-07-31：新增 `benchmark/` 单卡预训练 benchmark 脚本，默认测量 1000 steps，支持 10000 steps 长期测试。
- 2026-07-31：完成 benchmark 脚本静态审查；修正 DeepSpeed 脚本从配置读取 batch 与梯度累积参数并校验全局 batch，两个脚本固定 DataLoader 随机种子；三个 Python 文件通过 AST 语法检查，尚未在服务器 GPU 上运行。
- 2026-07-31：明确 `benchmark/` 默认使用 `data/seq_monkey/test_1000.jsonl` 作为快速测试子集；正式数据仍使用完整的 `mobvoi_seq_monkey_general_open_corpus.jsonl`，运行时通过 `--data_path` 指定。
- 2026-07-31：明确 benchmark 的预热步默认不计入稳定吞吐；后续结果应区分初始化与预热开销、稳定 step 吞吐，以及短任务所需的端到端总耗时。
- 2026-07-31：根据服务器 `wc -l` 结果确认完整预训练 JSONL 为 13,000,000 条，测试子集为 1,000 条；在全局 batch size 为 8 时，完整数据一轮对应 1,625,000 个 optimizer steps。
- 2026-07-31：修正 benchmark 的模型配置记录、词表和数据集校验、非法参数校验及进度输出；固定 benchmark 使用 `1024/18/16/8` 配置，并将实验元数据写入结果 JSON。
- 2026-07-31：修正 `LLaMA2.py` 的 `flash_attention` 开关，使 `flash_attention=False` 时真正使用手动因果注意力；完成 AST 和 JSON 静态复查，尚未执行服务器 GPU smoke test。
- 2026-07-31：补充 Attention 回退提示，区分主动关闭 Flash Attention 与 PyTorch 不支持 SDPA 的情况。
- 2026-07-31：明确 benchmark 脚本只用于固定步数的速度、吞吐和显存测量，不保存正式 Base checkpoint；真正的长时间预训练仍由 `pretrain_ds.py` 执行，而该入口当前默认使用 `LLaMA2.py` 的 `768/12`，与 benchmark 和 SFT 的 `1024/18` 尚未统一。
- 2026-07-31：确认当前 Plain PyTorch 与 DeepSpeed benchmark 都显式使用相同的 `1024/18/16/8` 模型配置、数据、Tokenizer、BF16、AdamW、global batch、序列长度和随机种子，因此 `pretrain_ds.py` 的旧 `768/12` 不影响这次性能对比；它只影响 benchmark 结果与正式 checkpoint 的对应关系。
- 2026-07-31：服务器 Plain PyTorch smoke test 已成功，峰值 allocated 约 4.16 GiB、reserved 约 4.77 GiB；DeepSpeed smoke test 在反向梯度分区阶段因 GPU 0 仅剩约 404 MiB 且被其他进程占用而 OOM，尚不能据此判断 DeepSpeed 代码失败。日志同时提示外层 `torch.autocast` 与 DeepSpeed 内置 BF16 重复配置，待确认后清理。
- 2026-07-31：确认在 `bf16.enabled=true` 时，DeepSpeed 会禁用外层 `torch.autocast` 并使用自身 BF16 设置；清理外层上下文不会改变当前有效数值精度，主要影响是消除警告和极小的上下文管理开销，暂不修改代码。
- 2026-07-31：按确认结果移除 `benchmark/pretrain_ds_bench.py` 中被 DeepSpeed 忽略的外层 `torch.autocast`，改用 DeepSpeed 内置 BF16，并在结果中记录精度控制器；尚未重新运行 GPU smoke test。
- 2026-07-31：复查项目训练链路；`pretrain_ds.py` 改为显式使用项目目标 `1024/18/16/8` 配置，读取并校验 DeepSpeed 的 micro batch、梯度累积和 world size，旧 `768/12` checkpoint 恢复时需显式传参。
- 2026-07-31：修正 `SFTDataset` 的 assistant loss mask，只在有效文本范围内查找 EOS，避免 padding 使用 EOS 时误标记，并处理回答被截断的情况；预训练和 SFT 数据集的行偏移改用紧凑数组保存。
- 2026-07-31：为 SFT 训练和推理增加 checkpoint 结构校验，补充模型参数、路径和训练参数校验；修正 tokenizer 训练进度长度使用文件字节数的问题，并补齐 Plain benchmark 的 BF16 控制器元数据。
- 2026-07-31：将 `LLaMA2.py` 的 `ModelConfig` 默认维度和层数统一为项目目标的 `1024/18`，避免未显式传参时误构造教程早期的 `768/12` 模型。
- 2026-07-31：明确 `pretrain_ds.py` 只负责正式 DeepSpeed 预训练、checkpoint 保存和恢复；Plain PyTorch 与 DeepSpeed 的速度、显存和吞吐对比只运行 `benchmark/pretrain_plain_bench.py` 与 `benchmark/pretrain_ds_bench.py`，两者都使用同一组 `1024/18/16/8` 配置。
- 2026-07-31：复查 benchmark 公平性，发现 DeepSpeed 配置启用了 `gradient_clipping=1.0` 而 Plain PyTorch 未执行梯度裁剪；已在 Plain 脚本中补齐，并让 DeepSpeed 脚本校验命令参数与配置一致。
- 2026-07-31：完成 Plain/DeepSpeed benchmark 第二轮静态审查；两套脚本的模型、数据、BF16、AdamW、学习率、梯度裁剪、global batch、序列长度、随机种子和计时方式已对齐，尚未运行服务器 GPU 实验。
- 2026-07-31：完成 Plain/DeepSpeed 单卡 smoke test；两份结果的配置和有效 token 数一致，1 step 下 DeepSpeed 比 Plain 慢约 11.9%，allocated 显存多约 1.35 GiB，reserved 显存多约 3.52 GiB。由于只测量 1 step，该结果仅用于验证流程和方向，不作为正式性能结论。
- 2026-07-31：将 Plain/DeepSpeed 单步 smoke test 的完整对比表写入 P1 记录区，原始 JSON 保持不变。
- 2026-07-31：记录 Plain PyTorch 1000-step 正式基线：0.5442 s/step、7511.90 nominal tokens/s、6070.54 valid tokens/s、4.1638 GiB allocated、4.8066 GiB reserved；DeepSpeed 1000-step 结果待补齐。
- 2026-07-31：记录 DeepSpeed ZeRO-2 1000-step 结果：0.6950 s/step、5882.43 nominal tokens/s、4753.73 valid tokens/s、5.5187 GiB allocated、8.2930 GiB reserved；单卡下比 Plain 慢约 27.7%，allocated 多约 32.5%，reserved 多约 72.5%。
- 2026-07-31：确认后续不通过限制显存或单纯增加 steps 来制造 DeepSpeed 优势；若继续验证，应新增 Plain DDP 与 DeepSpeed 双卡的等价 scalability 实验，保持 global batch size 一致。
- 2026-07-31：确定 DeepSpeed 优势验证顺序：先在单卡固定 global batch size 为 8，对 Plain PyTorch 和 DeepSpeed 成对测试 `micro/accumulation=1/8、2/4、4/2`；再新增双卡 Plain DDP 与 DeepSpeed ZeRO-2 对比，避免把增加 GPU 数量误认为框架加速。
- 2026-07-31：完成第一阶段 benchmark 脚本配置：Plain 与 DeepSpeed 均支持命令行设置 micro batch 和梯度累积，并强制单卡 global batch size 为 8；DeepSpeed 使用进程内配置覆盖，不修改基础 JSON。已补充 `4/2` smoke test 和 `2/4、4/2` 的四条 1000-step 正式命令。Python AST、编译、JSON、有效配置覆盖、batch 公式和 README 命令静态审查均通过；服务器 CUDA smoke test 待执行。
- 2026-08-02：服务器第一次运行 DeepSpeed `micro=4、accumulation=2` smoke test 时，参数解析提示不识别这两个参数；服务器脚本的帮助列表也未包含它们，而本地脚本已经在第 44、45 行定义。当前判断为服务器仍是旧版 `benchmark/pretrain_ds_bench.py`，需要同步本地新版后重新验证，不能将此次失败记为 DeepSpeed 或 micro batch 运行失败。
- 2026-08-02：同步新版脚本后，Plain 与 DeepSpeed 的 `micro=4、accumulation=2` smoke test 均成功。两份 JSON 配置字段和 3485 个有效 token 完全一致；单步中 DeepSpeed 的 nominal tokens/s 高 21.6%，allocated 显存多 1.0116 GiB。该结果仅用于确认可运行和观察方向，1000-step 正式比较待执行。
- 2026-08-02：收到 `micro=2/4` 和 `micro=4/2` 四份 1000-step 结果。六份单卡正式结果的共同配置、token 数和指标公式均通过核对；首轮最高吞吐为 `micro=2、accumulation=4`。文件时间显示两份 `4/2` 结果疑似重叠运行，其速度暂不作为最终结论，等待确认或顺序重跑；其余配置也需要重复实验后取中位数。
- 2026-08-02：用户确认两份 `micro=4、accumulation=2` 正式任务同时占用 GPU 0，该组 step time、吞吐和显存结果判定为无效，等待顺序重跑。`micro=2、accumulation=4` 未发现并发、配置或计算异常，保留为首轮有效结果。
- 2026-08-03：完成 `micro=4、accumulation=2` 顺序重跑。Plain 为 18773.98 nominal tokens/s，DeepSpeed 为 15424.93，DeepSpeed 低 17.8%且 allocated 多 1.0139 GiB。重跑配置、token 数、指标公式和文件时间均通过核对；`4/2` 成为两种框架当前吞吐最高的配置，最终中位数仍待另外两次重复实验。
- 2026-07-31：完成全项目 Python AST、JSON 配置和 JavaScript 语法检查；本地缺少 CUDA、PyTorch、Transformers 和 DeepSpeed，未在本地启动训练，服务器正式 1000/10000 steps GPU 实验仍待执行。
